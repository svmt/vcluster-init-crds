apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argocd-crds.fullname" . }}-test-connection"
  labels:
    {{- include "argocd-crds.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argocd-crds.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
